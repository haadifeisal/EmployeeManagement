apiVersion: v1
kind: Service
metadata:
  name: postgresql-db-svc
  labels:
    app.kubernetes.io/part-of: employeemanagement
spec:
  type: LoadBalancer
  selector:
    app: postgresql-db
  ports:
  - port: 5433
    targetPort: 5432